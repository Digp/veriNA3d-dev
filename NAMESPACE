import(bio3d)
import(parallel)
importFrom("graphics", "abline", "axis", "barplot", "contour",
       "legend", "par", "plot", "points", "text", "title")
importFrom("grDevices", "dev.off", "png", "rgb")
importFrom("methods", "new")
importFrom("stats", "complete.cases", "sd")
importFrom("utils", "download.file", "read.table", "setTxtProgressBar", "txtProgressBar")
importFrom(plot3D, persp3D)
importFrom(MASS, kde2d, bandwidth.nrd)
importFrom(jsonlite, fromJSON)
importFrom(RColorBrewer, brewer.pal)
importFrom(RANN, nn2)
importFrom(circlize, circos.par, circos.initialize, circos.trackPlotRegion, circos.axis, circos.trackPoints, circos.trackHist, circos.clear)

#exportPattern("^[[:alpha:]]+")
exportClasses(CIF)
export( 
    angles_dist, 
    applyToPDB, 

    binding.RNAprot,
    bp_hist, 

    checkNuc, 
    cifAsPDB,
    cifAtom_site, 
    cifAtom_sites,
    cifAtom_type,
    cifAudit_author,
    cifAudit_conform,
    cifCheck,
    cifChem_comp,
    cifDatabase_2,
    cifEntity,
    cifEntry,
    cifExptl,
    cifParser,
    cifPdbx_database_status,
    cifStruct,
    cifStruct_asym,
    cifStruct_keywords,
    classifyRNA, 
    cluster.select, 

    distances_entities,
    distances_eleno,

    eRMSD, 

    getAlternativeRepresentants, 
    getLeontisList,
    getNucData,

    hasHetAtm, 

    make_aantinfo, 
    measureNuc, 

    plot_circular_distribution, 
    plot.eta.theta2, 
    plot_et, 
    plot_hist, 

    queryAPI,
    queryAuthors,
    queryChains, 
    queryCompound,
    queryCompType,
    queryDepdate,
    queryEntities,
    queryEntryList,
    queryFormats,
    queryHeader,
    queryHetAtms,
    queryModres,
    queryNDBId,
    queryOrgLigands,
    queryReldate,
    queryResol,
    queryRevdate,
    queryTechnique,

    repsAsDataFrame,
    rvec_plot, 
    rVector, 

    select_ntID_neighbours,
    selectModel, 
    subset_bypucker, 
    subset_ntinfo, 

    trim_by_ntID,
    trim_sphere
)
